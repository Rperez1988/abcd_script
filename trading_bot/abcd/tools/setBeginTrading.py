def setDateTestingCanStart(counter, dateTracker, length, date):

    if counter == 0:
        
 
        dateTracker = str(date.date(length + length))
  
        counter += 1

    return counter, dateTracker